// install webpack with this command (cause this file haven't had webpack module yet):
// npm install --save-dev webpack-cli webpack

import "./styles.css";

const {DOM} = require('./DOMstuff');

const dom = DOM();

dom.startGame();


